// DAS MUSS IRGENDWANN RAUS
/*
export async function getAllPets(): Promise<AnimalsType[]> {
  const res = await fetch(`http://localhost:3000/api/animals`)
  if (!res.ok) {
    throw new Error(`Failed to fetch animals: ${res.status}`)
  }

  return await res.json()
}
*/
