{
  "name": "vetilib",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vetilib": "npm --prefix ./db run db && npm-run-all buildProject migrateDB seed && npm-run-all --parallel startBackend startFrontend",
    "startBackendFrontend": "npm-run-all --parallel startBackend startFrontend",
    "startBackend": "npm --prefix ./backend run start",
    "startFrontend": "npm --prefix ./frontend run dev",
    "buildProject": "npm --prefix ./backend install && npm --prefix ./frontend install",
    "migrateDB": "npm --prefix ./backend run prisma",
    "seed": "npm --prefix ./backend run seed",
    "vetilib:detached": "docker-compose up --build -d",
    "stop": "docker-compose down",
    "e2e-tests": "npx playwright test",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "cd backend && npm test",
    "test:frontend": "cd frontend && npm test",
    "prisma": "cd backend && npm run prisma"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^25.0.3",
    "npm-run-all": "^4.1.5",
    "sass": "^1.61.0"
  },
  "dependencies": {
    "@getbrevo/brevo": "^3.0.1",
    "bootstrap": "^5.3.8",
    "handlebars": "^4.7.8",
    "zod": "^4.1.12"
  }
}
