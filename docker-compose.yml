services:
  # PostgreSQL Datenbank Service
  db:
    build: ./db
    container_name: postgres-db
    environment:
      POSTGRES_USER: vetilib
      POSTGRES_PASSWORD: 123
      POSTGRES_DB: vetilib-db
    ports:
      - "5432:5432"
