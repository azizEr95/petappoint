[build]
buildCommand = "npm --prefix shared install && npm --prefix shared run build && npm --prefix backend install && cd backend && npx prisma generate"

[deploy]
startCommand = "cd backend && npx prisma db push && npx tsx scripts/seed-static.ts  && npx tsx scripts/seed-dynamic.ts && npx tsx src/index.ts"
