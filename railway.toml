[build]
buildCommand = "npm --prefix shared install && npm --prefix shared run build && npm --prefix backend install && cd backend && npx prisma generate && npm run build"

[deploy]
startCommand = "cd backend && npx prisma db push && node dist/index.js"
